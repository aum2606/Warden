name: second-orchestrator-delegation-is-allowed
input:
  principal: agent:orchestrator
  authority_chain: [user:aum, agent:orchestrator]
  tool: delegate
  params: {agent: ops-worker, scope: [github.issue.create]}
  context: {min_trust: trusted, sources: []}
  run: {run_id: run-delegate-2, elapsed_steps: 5, prior_denials: 0}
  environment: {current_time: "2026-09-24T10:01:00Z", org_settings: {}}
expect:
  effect: allow
  rule: delegate
  failed_conditions: []
