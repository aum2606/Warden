name: second-unknown-tool-is-denied
input:
  principal: agent:ops-worker
  authority_chain: [user:aum, agent:orchestrator, agent:ops-worker]
  tool: calendar.delete
  params: {event_id: event-2}
  context: {min_trust: trusted, sources: []}
  run: {run_id: run-default-calendar, elapsed_steps: 2, prior_denials: 0}
  environment: {current_time: "2026-09-24T10:17:00Z", org_settings: {}}
expect:
  effect: deny
  rule: default-deny
  failed_conditions: [default-deny]
