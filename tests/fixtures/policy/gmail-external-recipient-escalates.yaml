name: external-recipient-requires-approval
input:
  principal: agent:ops-worker
  authority_chain: [user:aum, agent:orchestrator, agent:ops-worker]
  tool: gmail.send
  params:
    to: [reviewer@external.example]
    subject: External review
    attachment_count: 0
  context:
    min_trust: internal
    sources: [chunk-1]
  run:
    run_id: run-mail-external
    elapsed_steps: 2
    prior_denials: 0
  environment:
    current_time: "2026-09-24T09:08:00Z"
    org_settings: {}
expect:
  effect: escalate
  rule: gmail-external-send
  failed_conditions: [recipient-domain]
