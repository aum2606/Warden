name: deny-dominates-size-escalation
input:
  principal: agent:ops-worker
  authority_chain: [user:aum, agent:orchestrator, agent:ops-worker]
  tool: gmail.send
  params: {to: [reviewer@acme.example], subject: Review, attachment_count: 4}
  context: {min_trust: untrusted, sources: [chunk-vendor]}
  run: {run_id: run-mail-internal-large-untrusted, elapsed_steps: 2, prior_denials: 0}
  environment: {current_time: "2026-09-24T10:06:00Z", org_settings: {}}
expect:
  effect: deny
  rule: gmail-external-send
  failed_conditions: [clean-provenance, size-bound]
