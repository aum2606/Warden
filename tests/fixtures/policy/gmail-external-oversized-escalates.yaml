name: external-oversized-clean-send-escalates
input:
  principal: agent:ops-worker
  authority_chain: [user:aum, agent:orchestrator, agent:ops-worker]
  tool: gmail.send
  params: {to: [reviewer@outside.example], subject: Review, attachment_count: 4}
  context: {min_trust: internal, sources: [chunk-2]}
  run: {run_id: run-mail-external-large, elapsed_steps: 2, prior_denials: 0}
  environment: {current_time: "2026-09-24T10:05:00Z", org_settings: {}}
expect:
  effect: escalate
  rule: gmail-external-send
  failed_conditions: [recipient-domain, size-bound]
