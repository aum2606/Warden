name: orchestrator-github-action-is-denied
input:
  principal: agent:orchestrator
  authority_chain: [user:aum, agent:orchestrator]
  tool: github.issue.create
  params: {repo: aum2606/Warden, title: Not authorized}
  context: {min_trust: internal, sources: []}
  run: {run_id: run-default-orchestrator, elapsed_steps: 2, prior_denials: 0}
  environment: {current_time: "2026-09-24T10:14:00Z", org_settings: {}}
expect:
  effect: deny
  rule: default-deny
  failed_conditions: [default-deny]
